name: continous-build
on: 
  push:  
    branches:
    - main
    - dev
  pull_request:
    branches:
     - main
  workflow_dispatch: 

# Testing our first workflow

jobs: 
  job1:
    runs-on: ubuntu-latest 
    steps:
        - name: step1
          run: | 
           echo "hello from step1, job1 of workflow1"
           sleep 30
           hostname
        - name: my step2
          run: |
            echo "hello from dan"
            sleep 10
        - run: |
            echo "hello from step 3"
            sleep 10
        - name: my step 4 for testing multiple lines of code execution
          run: | 
            ls -l
            pwd
            sudo cat /etc/os-release
            uname -a
            echo "final step4 execution done"
  job2:
    runs-on: ubuntu-latest 
    steps:
        - name: step1
          run: |
            echo "hello from step1, job2 of workflow1"
            sleep 30
            hostname
        - name: my step2
          run: |
            echo "hello from dan"
            sleep 10
        
